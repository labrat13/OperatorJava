Перепроектировать подсистему настроек, поскольку она сейчас не годится для использования.

- подсистема обслуживает ФайлНастроекОператора и ТаблицаНастроекОператора.

- ФайлНастроекОператора и ТаблицаНастроекОператора могут содержать несколько значений для одного ключа.
  Это реализовано как объект коллекции ключей SettingItemCollection на основе Map<List> коллекции. 

+ ключи для объектов ФайлНастроекОператора заданы енумом EnumSettingKey.
- ключи для объектов ТаблицаНастроекОператора заданы строками String.
- регистр символов для ключей не учитывается: Key = key = KEY.

- ФайлНастроекОператора не может содержать никакие другие ключи и настройки, кроме указанных в  EnumSettingKey.
  + функции должны использовать в качестве ключей элементов только EnumSettingKey.
  + основной код написан, но на авось. Не тестирован.
  - надо уточнить значения настроек по умолчанию в ApplicationSettingsKeyed.Reset()

- ТаблицаНастроекОператора предназначена для хранения настроек  и системных и пользовательских. 
  Поэтому она может содержать ключи из EnumSettingKey и произвольные ключи заданные пользователем.
  - эти множества значений ключей не должны пересекаться, поэтому нужна функция, проверяющая, что 
    ключ, созданный пользователем, входит/не входит в множество ключей из  EnumSettingKey.
    + для этого в енум надо внести 2 функции: 
      + Проверить, что ключ используется. 
        + EnumSettingKey.IsKeynameExists(String keyname); - сделано.
      + Получить массив строк ключей, используемых енумом.
        + EnumSettingKey.getKeyArray(); - сделано.
  - Функции должны использовать в качестве ключей только  String, не  EnumSettingKey. 
  + ТаблицаНастроекОператора в OperatorDbAdapter добавлена, функции написаны, не тестированы.
  
- Класс SettingItemCollection должен содержать все необходимые функции для работы с настройками.
  - это коллекция объектов SettingItem.
  - TODO: перечислить тут востребованные функции для работы с настройками:
    - toString() должен выводить состояние и количество элементов коллекции.        
    - get(EnumSettingKey key) должен возвращать массив/список элементов коллекции.
      - использоватьтолько в ФайлНастроекОператора.
    - get(String key) должен возвращать массив/список элементов коллекции.
      - использоватьтолько в ТаблицаНастроекОператора.
    - add(EnumSettingKey key, String value) должен добавлять значение в коллекцию как объект  SettingItem.
      - использоватьтолько в ФайлНастроекОператора.
      - TODO: допускается ли добавлять одинаковые объекты SettingItem?
    - add(String key, String value) должен добавлять значение в коллекцию как объект  SettingItem.       
       - использоватьтолько в ТаблицаНастроекОператора.
       
   - И там еще потребуется функция получения массива существующих имен настроек, чтобы пользователь
      ведь он же будет назначать настройки наобум через команды,
      вот чтобы он не назначил уже существующее имя на свою настройку, надо проверять это
      и писать, что идентификатор уже занят.     
          
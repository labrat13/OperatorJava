Надо попробовать добавить клипборд в Оператор
- как статический класс ClipboardManager
- Функции:
  - получить из клипборда веб-ссылку для скачивания wget.
    - будет использоваться внутри Процедуры.
  - записать в клипборд текст
    - пока не применяется нигде.

Замечания:
- данные в клипборде существуют, пока не закроется процесс - источник этих данных.
  Собственного буфера памяти у клипборда нет.
- в системе, по замечаниям в интернете, может существовать несколько системных клипбордов, какой из них используется - яхз.
  Но программы могут использовать известные им клипборды, а ява-программы - какие-то другие.
  Тут бардак, надо выяснять. 
- в отличие от винды, тут, в Линуксе, у каждого приложения свой подход к каждой проблеме, каждой фиче.
  Одних только оконных менеджеров тут десяток, у каждого свои условия, свои особенности.
  Поэтому все это такое замысловатое и кривое. Вот и клипборды могут работать, а могут и не работать в разных приложениях.


Решения:
- надо отработать эту фичу на прототипе:
  - получение веб-ссылки из файрфокса.
  - запись текста в клипборд и вставка его в разных текстовых редакторах.


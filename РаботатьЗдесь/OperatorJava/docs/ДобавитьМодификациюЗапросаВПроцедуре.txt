Добавить в Процедуру возможность модификации текста запроса.

- это еще одна новая фича, которой нет в Оператор-1.

- TODO: это надо добавить в теорию Оператора.
  - возможность модификации запроса для перенаправления запроса внутри Процедуры.
  - это событие модификации запроса должно записываться в лог: старое значение и новое значение.
    Иначе не разберешь, что там получилось в сложных случаях.

- проблема в том, что после модификации текста запроса надо заново запускать цикл выборки процедуры для запроса.
  А для этого Процедура должна вернуть специальный флаг результата, 
  его обработка приведет к перезапуску цикла выборки процедур,
  и тут легко ввести в бесконечный цикл всю эту систему при неправильном переопределении запроса.
  Поэтому предлагается эту возможность зарезервировать на будущие релизы.
  - но внедрить UserQuery можно уже сейчас, чтобы все методы библиотек Процедур уже использовали этот объект,
    и не пришлось все их переделывать потом.
    
- создать класс UserQuery, который будет хранить строку запроса и, заодно, операции с ней.
  + класс создан
  - операции добавить позже по месту.
+ заменить в прототипах библиотек Процедур String на UserQuery
+ заменить в коде вызова Процедур String на UserQuery.
+ добавить пояснение про модификацию шаблона в шаблон функции-Процедуры библиотеки Процедур.

+ для изменения запроса надо реализовать функцию public void ChangeQuery(Engine en, String q),
   чтобы гарантированно выводить в лог событие изменения запроса.
  Эту функцию придется вызывать из Процедуры (возможно, и из основного кода).
  + для лога нужна ссылка на движок.
  - можно добавить функцию в сам класс UserQuery, вместе с полем ссылки на движок.
    И автоматически писать в лог все изменения запроса.
  + добавлен код класса события лога для события изменения запроса: EnumLogMsgClass.QueryReplaced
  + функция реализована.  

На 19042022:
- запрос может быть модифицирован внутри Процедуры или внутри кода исполнения запроса.
- сообщение об изменении текста запроса пока не выводится в лог, но должно.
- перезапуск цикла выборки Процедур после изменения запроса не производится, 
  это делает изменение запроса внутри Процедуры нежелательным - нельзя гарантировать, 
   что новый запрос будет исполнен.

     
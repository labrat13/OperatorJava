
    //09022022 - Проблема с Terminal.getCursorPosition(r,c); 
    // Эта функция требует перевода терминала в не-блокирующий режим.
    // В нем функция System.in.read(); не ожидает завершения ввода клавишей Enter
    //  и вообще все немного иначе работает.
    // И еще - надо выключить на терминале echo, чтобы результат команды не отображался на экране.
    // Это требует либо весь код Terminal реализовать на С++ и подключать как библиотеку к Java машине, 
    // либо только инициализацию и отдельные команды реализовать и подключать как библиотеку к Java машине,
    // либо весь проект Оператор написать на С++ - это лучше, но дольше и сложнее.

Сейчас эта функция не используется - курсор просто переводится на новую строку.
Но это кривое решение - лучше чем ничего.
Надо реализовать все правильно для терминала.

Функция Terminal.getCursorPos()  используется в одном месте: DialogConsole.SureConsoleCursorStart().
Сейчас курсор там просто переводится на новую строку.
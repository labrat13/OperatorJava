Для функции String[] RegexManager.ParseCommandLine(String cmdline) и прочих функций Оператор:

1. Составить диаграмму процесса, как Оператор запускает файлы на исполнение в качестве процедур, как получает и использует сигнал о завершении и результат работы исполняемого файла.

2. определить, как вообще Оператор может запускать в Линукс файлы на исполнение в качестве Процедур.

3. Определить, какие именно типы файлов Оператор может запускать в Линукс  на исполнение в качестве Процедур.

4. Определить, как Оператор может передавать аргументы в такие исполняемые Процедуры.
а) через аргументы командной строки

5. Определить, как Оператор может получить сведения о результате исполнения Процедуры.
а) получить сигнал о завершени процесса приложения

6. Определить, как Оператор может определить тип файла.
- в гуе должны быть какие-то функции для этого.
 - но гуев в Линукс больше 5 (gnome, kde, lxde, xfce, ...), и у всех все как попало, наверняка.
- пользователь - создатель Процедуры должен сам подобрать процедуру, чтобы все работало. 
  - но нужна платформа для этого - ее и надо реализовать.

7. Найти аналог функции ShellExecute в Линукс.
- если такого нет (но он должен быть, для рабочего стола), придумать способ запускать файлы документов для просмотра. 

8. Найти способ, как определить, это путь к файлу или веб-адрес или еще что-то другое?
- чтобы правильно их обрабатывать.

9. Составить список вариантов для значений Мест:
- командная строка приложения для Терминала.
- путь к файлу документа.
- веб-адрес Интернета
- сетевой адрес локальной сети
- ... (Добавить сюда варианты)

10. Отработать все эти вопросы на прототипе и отобрать лучшие способы и код для использования в Оператор.
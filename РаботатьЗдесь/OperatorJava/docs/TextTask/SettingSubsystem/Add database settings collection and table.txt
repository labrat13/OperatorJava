+ Добавить класс кеш-коллекции значений, хранящихся в БД Оператор.
  + SettingItemCollection класс добавлен в ECM как часть.
- добавить использование его полей при загрузке Оператора, выгрузке, пред-обработке и пост-обработке команд.
  Как это планируется сделать для ФайлНастроекОператора.

Зачем он нужен?
- чтобы хранить произвольные пользовательские единицы данных сеанса Оператор.
- чтобы хранить пути процедур подобно тому как они хранятся в файле настроек Оператора, но по нескольку штук.


Придуматьтакой класс - адаптер для таблицы (ид; ключ; значение)
- явно его придется усовершенствовать по мере использования.

Вариант А) данные не хранить в памяти, а при каждом обращении к коллекции открывать соединение с БД и брать их из таблицы БД.
Вариант Б) данные загружать в память сразу при инициализации.

- при варианте Б такой класс после отладки можно добавить в мою библиотеку классов.
- ключи не уникальные в коллекции: под одинаковыми ключами может храниться сколько угодно значений.
  - но, возможно, это не будет часто использоваться в Оператор?
- регистр символов ключей НЕ учитывается.
- коллекция состоит из объектов  ид - ключ - описание ключа - значение. 
- ид применяется для точного указания объекта .
- набор функций - как у словаря настроек?
  - init(Engine en, DbAdapter db)
  - exit()
  - boolean hasKey(string key)
  - boolean hasValue(string key)
  - boolean hasID(int id)
  - int add(string key, string value) - return id
  - addIfNotExists(string key, string value)  - return id or throw
  - int delete(string key) - return number of deleted items
  - int delete(int id) - return number of deleted items
  - int replace(string key, string value)- return number of changed items ? todo: как производить замену здесь?
  - int replace(int id, string value)- return number of changed items
  - string[] get(string key) - получить массив значений по ключу
  - string get(int id)- получить одно значение по ид
  - Tuple get(int id) - получить настройку по ид
  - Tuple[] get(string key) - получить настройки по ключу
  - Tuple[] get(string value) - получить настройки по значению. 
  - int count(string key) - получить количество значений/настроек по ключу
  - todo: добавить возможные функции здесь.
  - todo: отобрать полезные функции здесь.